const express = require("express");
const app = express();

app.get("/", (req,res) => {
app.post("/", (req,res) => {
    req.params.articleTitle;    
    req.params.comments;    
    app.listen(3000);
});
});