const express = require("express");
const app = express();

app.get("/", (req,res) => {
app.delete("/", (req,res) => {     
    req.params.articleTitle,
    req.params.gifTitle,
    req.params.comment,    
    app.listen(3000);
});
});